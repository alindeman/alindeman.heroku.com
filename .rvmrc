
if [[ -d "${rvm_path:-$HOME/.rvm}/environments" \
  && -s "${rvm_path:-$HOME/.rvm}/environments/ruby-1.9.2-p136@alindeman.heroku.com" ]] ; then
  \. "${rvm_path:-$HOME/.rvm}/environments/ruby-1.9.2-p136@alindeman.heroku.com"
else
  rvm --create use  "ruby-1.9.2-p136@alindeman.heroku.com"
fi
